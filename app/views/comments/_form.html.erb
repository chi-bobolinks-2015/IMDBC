<%= form_for [@movie, @review, @comment] do |f| %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content, autofocus: true %>
  </div>

  <%= show_simple_captcha %>

  <div class="actions">
    <%= f.submit "Submit" %>
  </div>

<% end %>
